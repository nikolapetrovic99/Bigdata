
docker-compose up -d

u fajlu consumer_spark se pozicioniras i
docker build --rm -t bde/spark-app .



docker run --name proj3 --net bde -p 4040:4040 -d bde/spark-app

Instalacije na workerima:
docker exec -it spark-worker-2 bash
pip install influxdb
pip3 install influxdb-client
apk add --no-cache py3-numpy

Influx terminal - bash:
influx
use sparkbikesdb
select * from tabela